"""Built-in model plugins for `annolid.engine`.

Modules in this package should be safe to import even when optional heavy deps are missing.
Import those deps inside runtime methods (train/predict) and raise actionable errors.
"""

