# Boot Checklist

Run this checklist when the runtime restarts:

1. Confirm workspace templates are present.
2. Confirm `memory/MEMORY.md` and `memory/HISTORY.md` are readable.
3. Confirm configured skills can be listed.
4. Log startup state in `memory/HISTORY.md` if checks fail.
