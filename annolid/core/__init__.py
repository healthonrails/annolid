"""Core (GUI-free) primitives for Annolid.

The `annolid.core` package is intended to host reusable backend utilities and
typed data models shared between CLI and GUI entrypoints without importing Qt.
"""
